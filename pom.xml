<dependencies>
    <!-- JasperReports -->
    <dependency>
        <groupId>net.sf.jasperreports</groupId>
        <artifactId>jasperreports</artifactId>
        <version>7.0.2</version>  <!-- Dernière version -->
    </dependency>

    <!-- Pilote JDBC MySQL -->
    <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <version>8.0.41</version>  <!-- Vérifiez votre version MySQL -->
    </dependency>

    <!-- Commons Logging -->
    <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-logging</artifactId>
        <version>1.2</version>
    </dependency>

    <!-- Optionnel : Si vous utilisez d'autres fonctionnalités de JasperReports -->
    <dependency>
        <groupId>net.sf.jasperreports</groupId>
        <artifactId>jasperreports-fonts</artifactId>
        <version>7.5.0</version>
    </dependency>
</dependencies>